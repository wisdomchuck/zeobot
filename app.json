{
    "name": "zeobot",
    "description": "A discord bot based on heroku deployment",
    "image": "heroku/python",
    "repository": "https://github.com/heroku/heroku-buildpack-python",
    "keywords": [
        "python",
        "django"
    ],
    "env": {
        "token": {
            "description": "The secret key for the worker application.",
            "value": "${token}"
        }
    },
    "environments": {
        "dev": {
            "scripts": {
                "dev": "bash run.sh"
            }
        }
    }
}
